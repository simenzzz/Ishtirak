# Copy to .env and adjust for local runs. Never commit the real .env.
POSTGRES_PASSWORD=ishtirak

# Must be >= 32 bytes. Generate one with: openssl rand -base64 48
JWT_SECRET=dev-only-change-me-32-bytes-minimum-secret
