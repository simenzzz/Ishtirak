/**
 * Phase 5 replaces this shell with the operator dashboard and subscriber
 * portal routes. For now it is a placeholder that proves the build pipeline.
 */
export function App() {
  return (
    <main>
      <h1>Ishtirak</h1>
      <p>Generator subscription management — coming online.</p>
    </main>
  );
}
