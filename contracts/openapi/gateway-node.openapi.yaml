openapi: 3.1.0
info:
  title: Ishtirak gateway (public BFF)
  version: 0.1.0
  description: >
    The public API the web client consumes (ADR-003). The gateway authenticates
    every request (Bearer JWT), derives operatorId + role from the token, and
    aggregates/proxies to the internal core-java and analytics-python services.
    All resources are implicitly scoped to the caller's operator — clients never
    pass operatorId.


    The real-time channel (`GET /api/ws`, WebSocket upgrade) is **not** modeled
    in OpenAPI; its protocol lives in ../asyncapi.yaml and ../../docs/API.md.


    **Status:** every operation is implemented (`x-status: built`). The spec is
    not loaded into runtime validation — the gateway mirrors it with hand-written
    Zod schemas, kept aligned by the request-contract drift tests.
  contact: { name: Ishtirak, url: https://ishtirak.dev }
servers:
  - url: http://localhost:8080
    description: Local / docker-compose (public)

security:
  - bearerAuth: []

tags:
  - { name: auth }
  - { name: subscribers }
  - { name: tiers }
  - { name: readings }
  - { name: billing }
  - { name: payments }
  - { name: outages }
  - { name: analytics }
  - { name: health }

paths:
  /api/auth/login:
    post:
      tags: [auth]
      operationId: login
      summary: Exchange email/password credentials
      description: >
        Rate-limited (brute-force / credential-stuffing protection). The
        `password` field MUST never be logged (the gateway uses pino-http
        request logging — redact this body). Users are global by email and may
        have multiple operator memberships; multi-membership logins return a
        short-lived selection token instead of context-bound access tokens.
      x-status: built
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password:
                  type: string
                  format: password
                  description: Never logged or echoed back.
      responses:
        '200':
          description: >
            Authenticated or context selection required. On a direct (single
            membership) login the rotating refresh token is returned as an
            HttpOnly `Set-Cookie` (`ishtirak.refresh`), never in the body; the
            short-lived access token is in the body for in-memory use.
          headers:
            Set-Cookie:
              description: HttpOnly refresh-token cookie set on direct login.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [contextSelectionRequired, memberships]
                properties:
                  contextSelectionRequired: { type: boolean }
                  selectionToken: { type: string }
                  accessToken: { type: string }
                  memberships:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      required: [membershipId, operatorId, operatorName, role]
                      properties:
                        membershipId: { type: string, format: uuid }
                        operatorId: { type: string, format: uuid }
                        operatorName: { type: string }
                        role: { type: string, enum: [OPERATOR_ADMIN, OPERATOR_STAFF, SUBSCRIBER] }
                        subscriberId: { type: string, format: uuid }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '429': { $ref: 'components.yaml#/components/responses/TooManyRequests' }

  /api/auth/select-context:
    post:
      tags: [auth]
      operationId: selectAuthContext
      summary: Select an operator membership
      x-status: built
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [selectionToken, membershipId]
              properties:
                selectionToken: { type: string }
                membershipId: { type: string, format: uuid }
      responses:
        '200':
          description: >
            Context-bound access token in the body; the rotating refresh token is
            set as an HttpOnly `Set-Cookie` (`ishtirak.refresh`).
          headers:
            Set-Cookie:
              description: HttpOnly refresh-token cookie.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [accessToken]
                properties:
                  accessToken: { type: string }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/auth/refresh:
    post:
      tags: [auth]
      operationId: refreshToken
      summary: Refresh an access token
      description: >
        Authenticated solely by the HttpOnly `ishtirak.refresh` cookie — there is
        no request body. The gateway reads the cookie, forwards it to core-java,
        and sets the rotated refresh token as a new HttpOnly cookie. Refresh
        tokens are single-use and rotated on each refresh; detecting reuse of a
        consumed token revokes the token family (theft mitigation). Rate-limited.
      x-status: built
      security: []
      responses:
        '200':
          description: New access token in the body; rotated refresh token in the cookie.
          headers:
            Set-Cookie:
              description: Rotated HttpOnly refresh-token cookie.
              schema: { type: string }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [accessToken]
                properties:
                  accessToken: { type: string }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '429': { $ref: 'components.yaml#/components/responses/TooManyRequests' }

  /api/auth/logout:
    post:
      tags: [auth]
      operationId: logout
      summary: Log out — clear the cookie and revoke the refresh-token family
      description: >
        Clears the HttpOnly `ishtirak.refresh` cookie in the browser and, when a
        refresh cookie was present, calls core-java to revoke the whole refresh-token
        family server-side (best-effort: revocation failure is logged but still
        returns 204, so logout always succeeds for the browser). Idempotent;
        succeeds whether or not a session was active. Rate-limited.
      x-status: built
      security: []
      responses:
        '204':
          description: Cookie cleared.
          headers:
            Set-Cookie:
              description: Expired refresh-token cookie.
              schema: { type: string }
        '429': { $ref: 'components.yaml#/components/responses/TooManyRequests' }

  /api/me:
    get:
      tags: [auth]
      operationId: getMe
      summary: Current identity (operator, role)
      x-status: built
      responses:
        '200':
          description: The authenticated identity.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [operatorId, role]
                properties:
                  operatorId: { type: string, format: uuid }
                  subscriberId: { type: string, format: uuid, description: Present when the caller is a SUBSCRIBER. }
                  role: { type: string, enum: [OPERATOR_ADMIN, OPERATOR_STAFF, SUBSCRIBER] }
                  name: { type: string }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }

  /api/subscribers:
    get:
      tags: [subscribers]
      operationId: apiListSubscribers
      summary: List subscribers (paginated)
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      parameters:
        - $ref: 'components.yaml#/components/parameters/pageParam'
        - $ref: 'components.yaml#/components/parameters/limitParam'
      responses:
        '200':
          description: Page of subscribers.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, meta]
                properties:
                  data:
                    type: array
                    items: { $ref: 'components.yaml#/components/schemas/Subscriber' }
                  meta: { $ref: 'components.yaml#/components/schemas/PageMeta' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
    post:
      tags: [subscribers]
      operationId: apiCreateSubscriber
      summary: Create a subscriber
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [name, tierId]
              properties:
                name: { type: string }
                tierId: { type: string, format: uuid }
                meterId: { type: string }
      responses:
        '201':
          description: Created.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Subscriber' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/subscribers/{id}:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    get:
      tags: [subscribers]
      operationId: apiGetSubscriber
      summary: Get a subscriber
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      responses:
        '200':
          description: Subscriber.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Subscriber' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }
    patch:
      tags: [subscribers]
      operationId: apiUpdateSubscriber
      summary: Update a subscriber
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              minProperties: 1
              properties:
                name: { type: string }
                tierId: { type: string, format: uuid }
                status: { type: string, enum: [ACTIVE, INACTIVE] }
      responses:
        '200':
          description: Updated subscriber.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Subscriber' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/subscribers/{id}/readings:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
      - $ref: 'components.yaml#/components/parameters/pageParam'
      - $ref: 'components.yaml#/components/parameters/limitParam'
    get:
      tags: [readings]
      operationId: apiListSubscriberReadings
      summary: Reading history for a subscriber
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      responses:
        '200':
          description: Page of readings.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, meta]
                properties:
                  data:
                    type: array
                    items: { $ref: 'components.yaml#/components/schemas/Reading' }
                  meta: { $ref: 'components.yaml#/components/schemas/PageMeta' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/tiers:
    get:
      tags: [tiers]
      operationId: apiListTiers
      summary: List amperage tiers
      x-status: built
      responses:
        '200':
          description: Tiers.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: 'components.yaml#/components/schemas/Tier' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
    post:
      tags: [tiers]
      operationId: apiCreateTier
      summary: Create an amperage tier and tariff override
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [name, amperage, standingFeeUsd, standingFeeLbp, perKwhRateUsd, perKwhRateLbp]
              properties:
                name: { type: string }
                amperage: { type: integer, minimum: 1 }
                tariffPolicyOverride:
                  oneOf:
                    - { $ref: 'components.yaml#/components/schemas/TariffPolicy' }
                    - { type: 'null' }
                standingFeeUsd: { type: number, minimum: 0 }
                standingFeeLbp: { type: integer, minimum: 0 }
                perKwhRateUsd: { type: number, minimum: 0 }
                perKwhRateLbp: { type: integer, minimum: 0 }
      responses:
        '201':
          description: Created tier.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Tier' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/tiers/{id}:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    get:
      tags: [tiers]
      operationId: apiGetTier
      summary: Get a tier
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      responses:
        '200':
          description: Tier.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Tier' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }
    patch:
      tags: [tiers]
      operationId: apiUpdateTier
      summary: Update tier pricing, policy override, or status
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              minProperties: 1
              properties:
                name: { type: string }
                amperage: { type: integer, minimum: 1 }
                tariffPolicyOverride:
                  oneOf:
                    - { $ref: 'components.yaml#/components/schemas/TariffPolicy' }
                    - { type: 'null' }
                standingFeeUsd: { type: number, minimum: 0 }
                standingFeeLbp: { type: integer, minimum: 0 }
                perKwhRateUsd: { type: number, minimum: 0 }
                perKwhRateLbp: { type: integer, minimum: 0 }
                status: { type: string, enum: [ACTIVE, INACTIVE] }
      responses:
        '200':
          description: Updated tier.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Tier' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/readings:
    post:
      tags: [readings]
      operationId: apiRecordReading
      summary: Record a meter reading
      description: >
        Proxies to core-java, which emits `reading.recorded`. Staff can record
        normal forward readings; backdated or rollback corrective readings
        require an admin identity.
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [subscriberId, kwh, readingAt]
              properties:
                subscriberId: { type: string, format: uuid }
                kwh: { type: number, minimum: 0 }
                readingAt: { type: string, format: date-time }
      responses:
        '201':
          description: Recorded reading.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Reading' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/ingest/readings:
    post:
      tags: [ingest]
      operationId: apiIngestReadings
      summary: Ingest a batch of meter readings from a generator-site edge agent
      description: >
        Device-authenticated ingest. Unlike `/api/readings`, the credential is a
        device token (issued via `/api/devices`) carried in the `Authorization`
        header and authenticated by core-java — there is no user identity. The
        gateway validates the batch shape, rate-limits per device, and forwards
        the header through. Each reading is resolved by `meterId` to a subscriber
        and recorded idempotently; per-item failures do not fail the batch.
      x-status: built
      security:
        - deviceToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [readings]
              properties:
                readings:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items:
                    type: object
                    additionalProperties: false
                    required: [meterId, kwh, readingAt]
                    properties:
                      meterId: { type: string, minLength: 1, maxLength: 128, pattern: '^[A-Za-z0-9._:-]+$' }
                      kwh: { type: number, minimum: 0 }
                      readingAt: { type: string, format: date-time }
      responses:
        '200':
          description: Per-batch ingest verdict.
          content:
            application/json:
              schema:
                type: object
                required: [recorded, duplicates, errors]
                properties:
                  recorded: { type: integer, minimum: 0 }
                  duplicates: { type: integer, minimum: 0 }
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        meterId: { type: string }
                        readingAt: { type: string, format: date-time }
                        code: { type: string }
                        message: { type: string }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '429': { description: Rate limit exceeded. }

  /api/devices:
    get:
      tags: [devices]
      operationId: apiListDevices
      summary: List device tokens for the operator
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      responses:
        '200':
          description: Device tokens (secret omitted).
          content:
            application/json:
              schema:
                type: array
                items: { $ref: 'components.yaml#/components/schemas/DeviceToken' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
    post:
      tags: [devices]
      operationId: apiMintDevice
      summary: Mint a device token
      description: The plaintext `token` is returned only in this response and never stored.
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [label]
              properties:
                label: { type: string, minLength: 1, maxLength: 120 }
      responses:
        '201':
          description: Minted device token, including the one-time secret.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/MintedDeviceToken' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/devices/{id}/revoke:
    post:
      tags: [devices]
      operationId: apiRevokeDevice
      summary: Revoke a device token
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204': { description: Revoked. }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/billing-runs:
    post:
      tags: [billing]
      operationId: apiRunBilling
      summary: Trigger the monthly billing run
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [periodStart, periodEnd]
              properties:
                periodStart: { type: string, format: date }
                periodEnd: { type: string, format: date }
      responses:
        '202':
          description: Billing run accepted.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [issuedCount, needsReviewCount, periodStart, periodEnd]
                properties:
                  issuedCount: { type: integer, minimum: 0 }
                  needsReviewCount: { type: integer, minimum: 0 }
                  periodStart: { type: string, format: date }
                  periodEnd: { type: string, format: date }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '409': { $ref: 'components.yaml#/components/responses/Conflict' }

  /api/invoices:
    get:
      tags: [billing]
      operationId: apiListInvoices
      summary: List invoices
      x-status: built
      parameters:
        - { name: status, in: query, required: false, schema: { type: string, enum: [ISSUED, PARTIAL, PAID, VOID, NEEDS_REVIEW] } }
        - { name: periodStart, in: query, required: false, schema: { type: string, format: date } }
        - { name: periodEnd, in: query, required: false, schema: { type: string, format: date } }
        - $ref: 'components.yaml#/components/parameters/pageParam'
        - $ref: 'components.yaml#/components/parameters/limitParam'
      responses:
        '200':
          description: Page of invoices.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, meta]
                properties:
                  data:
                    type: array
                    items: { $ref: 'components.yaml#/components/schemas/Invoice' }
                  meta: { $ref: 'components.yaml#/components/schemas/PageMeta' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/invoices/{id}:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    get:
      tags: [billing]
      operationId: apiGetInvoice
      summary: Get an invoice
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      responses:
        '200':
          description: Invoice.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Invoice' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/invoices/{id}/reissue:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    post:
      tags: [billing]
      operationId: apiReissueInvoice
      summary: Re-issue a held invoice after corrective readings
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      responses:
        '200':
          description: Re-issued invoice.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Invoice' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }
        '409': { $ref: 'components.yaml#/components/responses/Conflict' }

  /api/invoices/{id}/void:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    post:
      tags: [billing]
      operationId: apiVoidInvoice
      summary: Void a held or unpaid issued invoice
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      responses:
        '200':
          description: Voided invoice.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Invoice' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }
        '409': { $ref: 'components.yaml#/components/responses/Conflict' }

  /api/invoices/{id}/payments:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    get:
      tags: [payments]
      operationId: apiListInvoicePayments
      summary: Payments recorded against an invoice
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      responses:
        '200':
          description: Payments for the invoice.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: 'components.yaml#/components/schemas/Payment' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/me/invoices:
    get:
      tags: [billing]
      operationId: apiListMyInvoices
      summary: The caller's own invoices (subscriber portal)
      x-status: built
      x-required-roles: [SUBSCRIBER]
      parameters:
        - $ref: 'components.yaml#/components/parameters/pageParam'
        - $ref: 'components.yaml#/components/parameters/limitParam'
      responses:
        '200':
          description: Page of the subscriber's invoices.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, meta]
                properties:
                  data:
                    type: array
                    items: { $ref: 'components.yaml#/components/schemas/Invoice' }
                  meta: { $ref: 'components.yaml#/components/schemas/PageMeta' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }

  /api/me/readings:
    get:
      tags: [readings]
      operationId: apiListMyReadings
      summary: The caller's own readings (subscriber portal)
      x-status: built
      x-required-roles: [SUBSCRIBER]
      parameters:
        - $ref: 'components.yaml#/components/parameters/pageParam'
        - $ref: 'components.yaml#/components/parameters/limitParam'
      responses:
        '200':
          description: Page of the subscriber's readings.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, meta]
                properties:
                  data:
                    type: array
                    items: { $ref: 'components.yaml#/components/schemas/Reading' }
                  meta: { $ref: 'components.yaml#/components/schemas/PageMeta' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/me/invoices/{id}:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    get:
      tags: [billing]
      operationId: apiGetMyInvoice
      summary: One invoice owned by the subscriber caller
      x-status: built
      x-required-roles: [SUBSCRIBER]
      responses:
        '200':
          description: Invoice.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Invoice' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/me/invoices/{id}/payments:
    parameters:
      - $ref: 'components.yaml#/components/parameters/idParam'
    get:
      tags: [payments]
      operationId: apiListMyInvoicePayments
      summary: Payments for one invoice owned by the subscriber caller
      x-status: built
      x-required-roles: [SUBSCRIBER]
      responses:
        '200':
          description: Payments for the invoice.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: 'components.yaml#/components/schemas/Payment' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/payments:
    post:
      tags: [payments]
      operationId: apiRecordPayment
      summary: Record a payment against an invoice
      description: Proxies to core-java, which emits `payment.received`.
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [invoiceId, currency, tenderedAmount, method]
              properties:
                invoiceId: { type: string, format: uuid }
                currency: { $ref: 'components.yaml#/components/schemas/Currency' }
                tenderedAmount: { type: number, minimum: 0 }
                method: { type: string, enum: [CASH, WHISH] }
      responses:
        '201':
          description: Recorded payment.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/Payment' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }
        '404': { $ref: 'components.yaml#/components/responses/NotFound' }

  /api/outages:
    get:
      tags: [outages]
      operationId: apiListOutages
      summary: Scheduled / active outages (operator or subscriber view)
      x-status: built
      responses:
        '200':
          description: Outages.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: 'components.yaml#/components/schemas/Outage' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
    post:
      tags: [outages]
      operationId: apiScheduleOutage
      summary: Schedule a load-shedding window
      description: Proxies to core-java, which emits `outage.scheduled`.
      x-status: built
      x-required-roles: [OPERATOR_ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [startsAt, endsAt, reason]
              properties:
                startsAt: { type: string, format: date-time }
                endsAt: { type: string, format: date-time }
                reason: { type: string, enum: [FUEL, MAINTENANCE, GRID, OTHER] }
      responses:
        '201':
          description: >
            Scheduled outage. Proxies core-java's create response, which mirrors
            the emitted `outage.scheduled` event payload (`outageId`, not `id`).
          content:
            application/json:
              schema: { $ref: '../events/outage-scheduled.schema.json#/properties/payload' }
        '400': { $ref: 'components.yaml#/components/responses/BadRequest' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/analytics/collection-rate:
    get:
      tags: [analytics]
      operationId: apiCollectionRate
      summary: Collection-rate summary
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      parameters:
        - { name: periodStart, in: query, required: false, schema: { type: string, format: date } }
        - { name: periodEnd, in: query, required: false, schema: { type: string, format: date } }
      responses:
        '200':
          description: Collection rate per period.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: 'components.yaml#/components/schemas/CollectionRate' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /api/analytics/risk:
    get:
      tags: [analytics]
      operationId: apiRiskFlags
      summary: Tampering risk flags
      x-status: built
      x-required-roles: [OPERATOR_ADMIN, OPERATOR_STAFF]
      parameters:
        - { name: subscriberId, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: minScore, in: query, required: false, schema: { type: number, minimum: 0, maximum: 1 } }
        - $ref: 'components.yaml#/components/parameters/pageParam'
        - $ref: 'components.yaml#/components/parameters/limitParam'
      responses:
        '200':
          description: Page of risk flags.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [data, meta]
                properties:
                  data:
                    type: array
                    items: { $ref: 'components.yaml#/components/schemas/RiskFlag' }
                  meta: { $ref: 'components.yaml#/components/schemas/PageMeta' }
        '401': { $ref: 'components.yaml#/components/responses/Unauthorized' }
        '403': { $ref: 'components.yaml#/components/responses/Forbidden' }

  /health:
    get:
      tags: [health]
      operationId: gatewayHealth
      summary: Liveness
      x-status: built
      security: []
      responses:
        '200':
          description: Service is alive.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/HealthStatus' }

  /ready:
    get:
      tags: [health]
      operationId: gatewayReady
      summary: Readiness
      x-status: built
      security: []
      responses:
        '200':
          description: Service is ready.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/ReadyStatus' }
        '503':
          description: Service not ready.
          content:
            application/json:
              schema: { $ref: 'components.yaml#/components/schemas/ReadyStatus' }

components:
  securitySchemes:
    bearerAuth:
      $ref: 'components.yaml#/components/securitySchemes/bearerAuth'
